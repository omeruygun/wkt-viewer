services:
  wkt-viewer:
    build:
      context: .
      dockerfile: Dockerfile
    image: wkt-viewer:latest
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
